# Verification: Validation Semantics and Media Crosscheck

## Scope and baseline

Feature 026 starts at `340ffd3`, Core 0.42.0, 256 materialized corpus cases and 34 active defect groups. The delivery branch reserves Core 0.45.0; CLI remains 0.1.3 and app remains 1.19.4. Main integration belongs to the maintainer.

Wave A repairs DEF-113/194/111/192/130 across eleven cases. Wave B remains in an independent scratch checkout until the first wave is committed and pushed. DEF-151 retains a separate recognition and preview dependency in peer-owned files; semantic fixes alone do not authorize its retirement.

## Wave A evidence

Seven new public crosscheck regression groups failed before implementation and pass afterward. A separate seven-group VAST suite covers structural evidence, protocol identities, alternate renditions, pod siblings, NonLinear scope and adversarial bounded inputs. Core, CLI and actual HTTP boundaries are exercised, including en/uk/ru findings. An independent review compared 96 price/floor/currency control pairs against the starting revision without an economic finding change.

The eleven candidate cases pass their Core requirements. Three unbound assertion symbols are bound to production IDs: `expected.crosscheck.seat.not_allowed` → `crosscheck.seat.not_allowed`, `expected.crosscheck.bid.audio_video_mediafile_mismatch` → `crosscheck.bid.audio_media_mismatch`, and `expected.crosscheck.bid.banner_content_mismatch` → `crosscheck.bid.banner_not_banner`. Only those exact IDs and proven expected-failure metadata may change. The oracle, payloads, levels, paths and parameter requirements remain intact.

Full Core/HTTP corpus: 609 tests, 514 passes, 92 expected-failure markers and three declared Core non-applicable skips; no unexpected failures. All eleven scoped cases pass at Core, HTTP and browser layers. The full browser corpus records 196 normative passes, 59 preserved known deviations and one HTTP-only non-applicable case. All 245 unrelated Core/HTTP case signatures are byte-identical to the starting observations.

The isolated `npm run ci` passed all 166 ordinary test files and 25 serial browser files without a browser-file retry. The semantic runtime was settled for this gate. The permanent brief then amended the release reservation from 0.44.0 to 0.45.0 because PR #82 owns 0.44.0; the metadata-only correction receives the version/governance checks before delivery. Package smoke installs Core 0.45.0 with CLI 0.1.3 and validates its real exit policy. Docker smoke passes health, analysis, content-hashed assets and native module loading in a disposable container. Hosted verification passed on wave A commit `a2a629d` in [PR #83](https://github.com/vladikkrasulya/adtech-spyglass/pull/83), [CI run 34257590784](https://github.com/vladikkrasulya/adtech-spyglass/actions/runs/34257590784).

## Wave B evidence and retained dependency

The bounded response patch and Native crosscheck patch joined only after wave A was pushed. Their initial tests failed before repair, and 42 integrated public-boundary groups now pass. The full Core/HTTP corpus passes with 514 ordinary passes, 58 expected-failure markers and three declared non-applicable Core skips; no unexpected failures. The complete real-loader integrity comparison against `f1758bc` preserves all 256 materialized payloads and 231 unrelated cases, including exact metadata and failure signatures. Twenty scoped cases are normative; five retain only the original DEF-151 recognition/preview deviations.

Candidate retirements are DEF-195/150/190/198/109/170/301/197, in addition to the five wave A groups. DEF-151 remains open: its five responses validate as structured AdCOM Native and receive the required asset crosscheck, but `detectFormat` still reports banner and the actual UI preview remains empty on this branch. Only its two repaired semantic signature patterns per layer are removed; the original format, preview, image-count, image-load and identity signatures remain exact. No payload, severity, path, parameter assertion or corpus oracle changes. Four wave B `expected.*` symbols bind to the corresponding production IDs, for seven explicit bindings across the feature.

Independent review added regressions for falsy/malformed Native containers with optional requested assets and whitespace image URLs; neither may receive `native_complete`. Embedded base64 PNG/JPEG/GIF/WebP image URLs are accepted only at image fields. Navigation schemes and data HTML/SVG remain diagnosed; this syntax check does not claim the image bytes decode, which remains an observable preview check.

An external proposal for the DEF-151 owner touches only `format-detect.js` and the existing app Native input adapter. It has not been applied to this delivery checkout. Real-loader impact is confined to the five cases' ten detection observations, with synthetic DOM classification/rendering controls passing; it does not claim actual browser image loading. The current permanent brief assigns other runtime files to the maintainer, so full retirement awaits that owner's integration and the unchanged five-case browser proof. The settled wave B `npm run ci` passes 168 ordinary files and 25 serial browser files without a browser-file retry. Its browser corpus records 214 normative passes, 41 preserved known deviations and one HTTP-only non-applicable case; each of the five DEF-151 cases retains exactly its six original format/preview/identity failures. Package smoke and Docker smoke pass on Core 0.45.0/CLI 0.1.3, including real CLI exit behavior, container health/analysis, hashed assets and native module loading. The final hosted check starts when this wave is pushed; PR #83 and the external immutable delivery receipt provide its SHA and result. No further runtime edits followed the settled gate.

## Evidence location and isolation

Durable local evidence: `~/.local/share/ortbtools-research/2026-09-08-026-validation-crosscheck/`. This includes the real-loader baseline snapshot, initial failures, focused test logs, exact ID binding map and independent wave B patch receipt. Browser runs use private profile prefixes and a private PID namespace, so cleanup cannot target peer browsers. This changes process ownership only, never assertions or product inputs.

## Delivery

Wave A was rebased before its first push onto `f1758bc`, which integrates peer features 024/025 (Core 0.44.0). Their eight retired groups and all contract/resolution entries are preserved. The rebased baseline has 26 groups; wave A leaves 21. A fresh real-loader baseline confirms all 245 unrelated Core signatures remain identical, and 649 focused/corpus checks yield 570 passes, 76 expected-failure markers, three declared skips and zero failures. Rebased lint, typecheck and version/governance gates pass. The initial full local coverage/browser gate and the subsequent rebase checks are recorded separately, without claiming that the initial gate ran on the rebased commit. The second delivery retains accurate thirteen-group retirement accounting, unchanged unrelated corpus signatures and settled local gates. Post-commit hosted status is recorded through the PR checks and external receipt. Full feature completion additionally requires T025/DEF-151 owner integration; that unmet observation is not hidden by successful delivery gates.

## Convergence assessment

SpecKit convergence checked 17 functional requirements, four success criteria, eleven acceptance scenarios, four delivery/architecture decisions and eight constitution principles. One HIGH partial implementation finding remains: the peer-owned DEF-151 recognition/preview repair. It is appended as T025, with exact preserved expectations and a concrete external proposal. No additional missing, contradictory or unrequested implementation was identified in the assigned Core files. The feature remains In Progress rather than falsely claiming fourteen complete groups; pending delivery gates remain listed separately in the existing tasks.

The completed wave B scratch checkout was archived with its authored source, patches and a verified archive hash, then removed. The delivery checkout and all receipts remain available; peer checkouts were left with their owners.
