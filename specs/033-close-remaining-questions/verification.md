# Verification — remaining product questions

## Pre-implementation evidence

Baseline2667412 clean; production remains app1.22.0 image9899191. Spec Kit0.16.2 and empty hooks verified. Read-only owners investigated all eight stories. Temporary real SQLite reproduces logout revival; held sample response reproduces premature empty Analyze; synthetic push document reproduces loss of head styles during successful inlining. Conditional Cloudflare injection is present in actual browser HTML but not repository source.

Pinned dialect audit4052-file digest and retained source match. Official Go1.25.0 and isolated dependencies are prepared; network-none module verification passed. The nineteen witnesses have not yet run.

Cloudflare requires user login in the opened browser; a text request is pending while independent work continues. Native browser/zoom/reader/device execution remains pending. No pending check is recorded as success.

Fresh security-review-agent creation returned “agent thread limit reached”. Root traced auth/storage/callers independently and a separate read-only investigator challenged the proposed boundary, finding shutdown ordering, anonymous journal growth, rollback identity and concurrent reset/login requirements. This is the fix-finding fallback, not a claim that a new isolated agent ran.

## Implementation gate

Read-only independent cross-artifact analysis found full coverage of18FRs and7SCs. Two medium gaps (explicit reset/login+shutdown race regressions and numeric journal bounds) were amended and independently rechecked with no remaining reportable findings. Both manifests are frozen and parse:19unique witness IDs (7+12),540layout cells plus separate journey/zoom/reader/device rules. Requirements checklists pass; Spec Kit contract10/10 passes. Product implementation started only after this gate. Existing ignore policies cover task-local data, secrets and generated artifacts; no ignore change is needed. Hooks remain empty.

## US7/US8 implementation evidence

T031–T033 and T036–T039 are verified. Pure SChain inspection accepts bounded structured, serialized and query forms without fetching, preserves current/legacy/OpenRTB3 locations, compares only structurally valid copies, distinguishes seller account case and reports factual node counts. Optional rid/domain guidance retains its IDs at INFO. Declared sender and route context is explicit; missing or unsupported context remains unknown. The catalog contains14 pinned profiles and19 source-backed statements in EN/UK/RU.

Focused command `node --test tests/inspection.test.js tests/rules-etap-b.test.js tests/spec-refs.test.js tests/finding-id-policy.test.js tests/dialect-witness-receipt.test.js` passes139/139 with no skips or retries. Owned ESLint/Prettier and whole-worktree `npm run typecheck` pass. Receipt tests bind current harness bytes and profile source hashes to executed evidence; SChain tests cover malformed inputs, decoding, copy order, duplicate identities, declared-context controls and OpenRTB3 referenced paths. These scoped checks do not replace the pending integrated/release gates.

Direct witnesses ran against Prebid Server `0ba352315253f6692af6497d553cfb12909a1b8b` with verified Go1.25.0. Actual attempt004 isolation records network=none, read-only source/root, uid1000 and dropped capabilities. All19 frozen IDs pass:69 variant executions and181 assertions. Command: `python3 harness/run.py /tmp/ortbtools-dialect033-kplg6vsh 4`; amendment: `python3 harness/amend.py /home/vk/.local/share/ortbtools-research/prebid-2026-08-20 /tmp/ortbtools-dialect033-kplg6vsh 4`. The harness makes no HTTP calls. Raw logs, cloned synthetic inputs, per-ID triggers/controls and unsuccessful attempts001/002 are retained in the external bundle `remaining-questions-20260909/d19/`. Attempts003/004 pass;004 additionally verifies source/membership before execution.

[witness-results.json](witness-results.json) records every before/after disposition. Four classifications change;17 existing corpus rows receive19 proofs. The new derived corpus SHA256 is `7e0a96eee1a2c0ee549a5051518c8f3e8af72f95fc5d39bd75b7ab5d0c703bc4`. Historical008 result36pass/12inconclusive remains scoped to its original auction generation. A post-execution read-only check confirms the original4052-file audit digest remains `edf782a09fdf3180feb9077b360d96eceab5a316f12bc342fae6376c118d9230`; original corpus SHA256 remains `73d067fa6ea9689b09167104db7bb1a72ff950446db8275b41bd54e32193598b`. This evidence supports the nineteen bounded questions and declared-profile applicability, with no actual-traffic or corpus-wide accuracy claim.

## Session and sample readiness implementation

US1 focused actual-process recovery/reset tests plus existing auth/DB/HTTP/privacy/session regressions pass188/188. The new slice covers deletion failure with second-user continuity, crash and clean restart, exclusive ownership, cap/oversized/corrupt/missing state, keyed lookup/old-image behavior, reset/login proof races and shutdown drain. `033-auth-focused.log` and `033-auth-typecheck.log` retain the output in the external bundle. Independent auth review T010 remains pending until its separate reviewer completes.

US2 Clear passes2/2 on its first browser attempt with the sample held beyond the former1200ms assumption. Readiness follows actual response, matching editors and enabled Analyze; clear/error assertions remain. `033-clear-browser.log` retains the outcome. This establishes the controlled failure path; the original hosted CI timeout's exact timing cause cannot be reconstructed from its incomplete old log.

## Cloudflare configuration (R03)

The owner supplied the signed-in in-app browser. Account Analytics → Web analytics → Manage site for `ortbtools.com` showed automatic RUM enabled. Selected Disable, saved Update, then reloaded the page: configured hostname remained `ortbtools.com`, Disable1 and all three Enable options0. No other site setting changed. `edge/cloudflare-setting-readback.json` records the UI observation.

Fresh actual Chrome152.0.7977.83 public response is200 with no injected Cloudflare beacon and no console/page errors. Application CSP and `Cache-Control:no-cache` remain unchanged; `/telemetry.js` loads200 and two first-party event requests return204, marked internal test traffic. HTML and exact readback are retained in `edge/fresh-chrome-response.html` and `edge/fresh-chrome-evidence.json`. R03 configuration and runtime checks pass.

## Inspection and interface regressions

Independent review reproduced a valid inspection misreported as400 on unexpected Core failure and two stale-response UI races. Both inspection HTTP routes now return static500 `inspection_failed` for unexpected backend failure; typed input/context errors remain400. Dialog input/sender edits invalidate pending inspection; changing applied declarations invalidates the old main analysis and derived output. HTTP5/5 and actual Chrome2/2 pass, including EN/UK/RU and held-response controls. Source-link scheme/escaping injection controls pass. Exact before/after output is retained as `033-inspection-*.log`.

Saved-library503 previously displayed an encryption-lock claim, and logout left cached prior-account rows visible. Retry/error handling and auth-generation invalidation repair both. A held A response cannot overwrite account B after logout/login. History quota recovery previously left50 visible rows against25 in-memory entries; persistence now completes before paint. The focused Chrome suite covers fourteen frozen corpus witnesses, ring50, reload, two-tab propagation, quota, clear-versus-server-record isolation, long literal title, partner navigation and library retry/logout. Separate actual drawer journeys pass in EN/UK/RU for locked/unlocked, legacy, filters, load/edit/delete, empty active filter and another-account404. Failed diagnostic attempts remain: background-tab click needed explicit tab focus, initial drawer action needed expanded-state readiness, and per-locale server isolation avoids the real sign-up rate limit. Native family/viewport/zoom/reader execution is recorded separately; these Chrome regressions do not imply those cells passed.

Final saved/history Chrome suite:5/5 PASS,0 skips,43.59s, including deletion to a fully empty account Library in all three locales. `033-saved-history-final.log` retains this outcome. Root-owned ESLint and whole typecheck pass; Spec Kit contract10/10 passes after documentation/version updates.

## Independent auth and UI receipts

T010 independent reviewer traced the final auth patch, all production mint/reset callers, dirty-before-hydration, journal-before-delete, HMAC-only lookup, uncertainty invalidation, credential-proof fencing and drained shutdown. No actionable bypass remained. Independently rerun actual process/recovery/reset tests33/33 pass. Backup/restore/login-loss consequences are documented in the current operations and retention contracts.

D16/D17 focused nonbrowser tests99/99 pass; combined preview gate9/9 passes, and the final all-locales mapping/assets browser regression1/1 passes. Actual decoded images, opaque pixels,240/48px CSS dimensions, push48px icon, selected-second request identity and no advertiser browser requests/pageerrors were measured. Subsequent screenshot review repaired HTML5 doctype placement, hidden retry/cancel/value controls, toast hit interception and a1024px Account mapping row overflow. The final long-path regression checks card edges and list overflow in all three locales. The separate UI evidence bundle is `.codex/maintenance-20260909/033-ui-final/` in the original checkout: `focused-tests.log`, `browser-final2.log`, `layout-final.log` and the responsive/Native/push PNGs. Earlier attempts remain beside them.

The test runner now classifies shared `launchBrowser` callers into the serial browser partition. Its10/10 lifecycle regressions pass; measured partition189unit files and30browser files. Root integrated format/lint/typecheck diagnostics pass. The immutable Linux snapshot/full gates are the next evidence authority; these diagnostics do not claim deployment.

## Native desktop layout execution

T018 passes all540 frozen layout cells: Chrome152.0.7977.83, Firefox155.0.1 and Safari26.6.2 each pass180 viewport/locale/theme/surface combinations. Actual CSS viewport readbacks match all ten dimensions from1024×768 through3840×2160; Safari screenshots use actual DPR2 and the other engines DPR1. The harness verifies loaded fonts, local typography hierarchy, saved/mapping fixture counts, document overflow and individual actionable right edges/ancestor clipping. Measured UI text sizes remain11–20px, including13px saved rows and approximately13.2px mapping rows. Fonts and source hashes are retained per cell/run.

A real1024px account defect was found before the settled run: the mapping list shared a horizontal action row and extended beyond the viewport. The UI owner moved it into a bounded wrapping block; the subsequent540-cell execution passes. Six representative final screenshots across minimum width,1280/1366 breakpoints and larger layouts were visually inspected, alongside pilot extremes at3840px. The screenshots preserve genuine native rendering; no physical panel or zoom outcome is inferred.

[layout-results.json](layout-results.json) enumerates every actual cell and links the full measurements/screenshots in `remaining-questions-20260909/native-matrix/attempt001/`. All tracked visual source hashes are unchanged between each engine's run start/end. Prior pilot failures remain available: a Firefox DOM-element predicate serialization issue, Safari background painting and a native interaction window preparation issue. Behavioral journeys, actual zoom, screen reader and physical-device prerequisites remain separate execution gates.

## Subsequent defects and server-only execution

Distinct encrypted/legacy fixtures exposed a real legacy-load defect: absent IVs were passed to decryption, yielding empty request/response editors after a success toast. Each body now decrypts only when its own IV exists. Readiness binds the selected sample ID and both distinct editor bodies. The English pre-fix browser regression fails; the corrected EN/UK/RU journeys and crypto controls pass18/18. `033-legacy-load-before.log` and `033-legacy-load-after.log` retain both outcomes.

Independent route review reproduced stack exhaustion with a JSON-representable10000-level impression array and cyclic pure-Core carriers. Route relevance now rejects malformed impression entries and uses an iterative shared10000-value/32-level traversal bound; exceeded bounds return `unknown/incompatible_input`. Unrelated extension subtrees are not scanned. Focused inspection/HTTP/witness controls pass29/29, including deep JSON, cycles and shared acyclic controls; prior failure is retained in `033-route-bounds-before.log`.

On2026-09-09 the owner required all further browser tests to run on vkbox during their call. Root's owned Chrome, Firefox, synthetic local app and SafariDriver processes were stopped; the other native-matrix owner confirmed all owned local browsers/apps had exited. No VoiceOver session was enabled. Completed macOS receipts remain historical evidence; further browser runs use only isolated server environments. Native Safari behavior/zoom, actual VoiceOver and real-device execution remain explicit prerequisites when unavailable there. They are not inferred from Linux rendering or accessibility trees.

The pre-transfer Chrome native-zoom run executed30 actual Appearance-setting cells with DPR/viewport readback and keyboard focus geometry. Twenty-six pass; four Account cells expose partially clipped focused links in its horizontal navigation at200/400 percent. These failures are retained under `native-zoom/chromium-final-fresh/` and must be superseded by the focused repair and server recheck, not reported as a complete zoom pass.

## Final integration review and focused server regressions

Independent inspection integration review closed six reported presentation/error-path defects: completed sender-only comparisons, full conditional adapter statement text, localized input format, successful-envelope validation before result paint, previous-route preservation after catalog failure, and clearing stale comparison/route output from an open dialog. Context-preserving Apply retains a completed result; changed context, editor input and clear invalidate both sender and route output. Source URLs remain HTTPS-only and escaped. The exact-module JSDOM/code-trace receipt `033-inspection-independent-final.log` passes all reported controls; it does not stand in for a real browser.

The vkbox real-browser before run fails2/2 against the earlier inspection/app source: missing localized serialization label and a malformed success response retaining a known-route claim. The current presentation/account server run passes4/4, covering all three locales, sender-only and combined declarations, mixed audio/banner conditional guidance, held responses, malformed success/catalog controls, navigation collapse/Escape and90 Account Tab/Shift+Tab full-link-rectangle checks at427/854px. A final affected inspection rerun after the open-dialog clearing correction passes2/2 in10.17s without retries. The intermediate unsupported Puppeteer `Shift+Tab` shortcut is retained as a harness failure and replaced with actual Shift down/Tab/up. Browser execution took place on vkbox; no local browser was launched after the owner's constraint.

The final runtime verification snapshot is baseline2667412, tree `aea968e26aed6b9fdaf4a1bddb0e54740697c136`, binary patch SHA256 `f0fb06368b9947a3a58cef694f86fcee852c7c9b2bbc8a82e5e8d9b3fb8a79cb`. It contains104 changed/added files and1,052,965 patch bytes. Local combined Prettier/ESLint/typecheck passes; subsequent changes are execution receipts unless explicitly recorded otherwise. The immutable Linux full gate and server browser matrix must report their own completion before release.

## Server native zoom and behavioral receipts

Chrome151.0.7922.137 on vkbox passes30/30 actual native zoom cells across100/125/150/200/400 percent, both themes and Inspector/saved Library/Account. The harness uses Chrome Appearance settings with actual viewport/DPR readback, a1920×1080 native window and CSS zoom1; no CSS transform or emulated viewport substitutes for zoom. Each cell records60 native Tab steps, for1800 total, with actual document focus and complete control-rectangle visibility. Browser-chrome/body stops are recorded separately. Source hashes are unchanged before/after the run. `native-zoom/chromium-server-final/results.json` SHA256 `4f37bcda3b724f433e04181b5a65e2dcac85f7e82979d091450fa3b1521b400a` and30 PNGs retain the result. Representative saved-Library100% and Account400% screenshots were visually inspected. This supersedes the four earlier clipped Account focus failures.

On the same runtime snapshot, native server Chrome and Firefox155.0.1 each pass the five grouped saved/history tests at1366px and1920px, for20/20 grouped tests with0 skips. Mapping lifecycle passes all24 browser/width/locale/theme cells, with12 assertions per cell. The frozen journey ledger maps this evidence to40 passed Chrome/Firefox rows and20 explicitly open Safari rows; those40 rows are not40 independently executed test files. `native-matrix/journey-results-server.json` SHA256 `b5ded858aa486a0275848f574f95b273fbccc5cee44d4b8077ee9b364921ae13` retains the mapping and original command logs.

An authorized physical Mi9T attached to vkbox supplies the available-device prerequisite: Android16, Jelly16 and Chromium/WebView151.0.7922.71. Device identity and inventory are recorded without its hardware serial. Physical execution found a saved-Library row linking to an unhandled Account sample anchor; the earlier browser test only asserted the hash and did not prove that the selected example opened. This is an actual navigation defect under FR-006. Its bounded repair and complete editor-body regression are pending; the earlier runtime snapshot remains valid evidence for its own bytes, not for that later repair.

Accordingly, the current matrix supersedes the four Chrome/Firefox navigation rows from that older40-pass receipt:36 passed,4 failed navigation rows pending repair,20 unavailable Safari rows. The original receipt is retained as a test outcome, without treating its insufficient hash assertion as proof that the user journey worked.

Firefox155.0.1 on vkbox also passes30/30 native zoom cells. Its isolated native zoom list is explicitly configured to100/125/150/200/400; actual Ctrl+0/Ctrl+plus input, toolbar zoom badges, DPR and viewport readbacks confirm each level, while CSS zoom remains1. All18 recorded source hashes are unchanged. The1800 Tab observations contain826 actual document-focus observations and974 outside-document/browser-chrome stops. Four raw stale-element rectangle flags occurred while the document did not own focus; they are retained and separately classified, with0 actual-focused control failures. `native-zoom/firefox-results-server.json` SHA256 `f2983b2d7b1c787d309755f8456a72fcbafb4150419ad08d181c2f9ff7563405` includes both immutable run partitions and their harness/PNG hashes. Representative400% saved Library and Account native-window screenshots were visually inspected. All owned server zoom processes were closed after execution.

## Hosted gate capacity

The actual successful baseline2667412 hosted run [34340919725](https://github.com/vladikkrasulya/adtech-spyglass/actions/runs/34340919725) took14m23, including13m10 in Tests, leaving only37seconds inside the former15minute job limit before the additional033 controls. The CI job limit is raised to25minutes. All checkout, install, format, lint, typecheck, tests, package and Docker steps remain required; neither a test timeout nor a failed assertion is relaxed. The final hosted run remains an independent required gate for its exact commit; its first actual result is recorded below, and it failed for an unrelated reason.

## Saved navigation repair and physical coverage boundary

The saved-Library link now targets Inspector with one explicit positive safe-integer ID. Locked reload retains the selected record until the existing localized unlock flow succeeds, then consumes the query once. Ambiguous saved/curated or duplicate IDs are rejected without a source fetch. Missing/foreign records remain404 at the account API; malformed responses cannot produce a success toast. A generation, initiating-user and editor snapshot check prevents late fetch/decrypt completion from replacing a newer selection, typed content or another account. Canonical Clear also invalidates empty-to-empty delayed loads.

The actual server before test times out waiting for Inspector after the original Library link. After repair, eight unchanged saved/history cases pass, and two negative/race cases pass15.24s after correcting their test-only double request interception. This is combined10-case evidence, not a single uninterrupted10-case run; both failed attempts are retained in `033-saved-handoff-server-before.log` and the original after log. All three locales check literal translated unlock title/subtitle, locked reload, exact encrypted and distinct legacy bodies, and query consumption. Root and the separate Linux-gate owner reviewed the final handoff source with no remaining actionable finding. Scoped formatting/lint, whole typecheck and diff checks pass. The final full CI and amended native journey runs must verify the complete settled patch.

The physical Mi9T produced actual UK and EN saved-drawer lock/unlock/filter/encrypted/edit/legacy passes, plus UK history analyze/persist/reload/load/delete and saved-record isolation with0 page errors. The measured viewport was851×274 CSS pixels atDPR2.75/scale1; no emulation or device settings were applied. Initial malformed-selector, keyboard-obscured tap and readiness attempts remain in the raw logs; the dead Library link was a product failure and led to the repair above. After another foreground app made the test WebView unavailable, further physical execution stopped. Only the proven synthetic browser task, owned forwards/reverse and isolated server app were cleaned up, without foregrounding any app. EN history and all RU flows, plus physical post-fix Library navigation, remain open. Desktop checks cannot close those physical observations.

## Convergence and final source boundary

`speckit-converge` prerequisites resolve this033 package. Root and a separate read-only reviewer checked18 functional requirements,7 success criteria,31 acceptance scenarios, four ownership streams and all eight constitution principles against the implementation and retained reviews. No missing/partial/contradictory/unrequested buildable work remains, so convergence appended no tasks and left the task file unchanged during assessment. This implementation outcome does not close T019's unavailable execution cells or anticipate the pending release gates. The existing T047 owns final evidence/wording reconciliation.

The settled post-handoff snapshot is tree `4379e7d54010787942764d047bd9acb1f346100d`, patch SHA256 `3ad6329b39dab6569e033043e1c29263ec8eecc325f0adf0847867a8576c5c39`,1,164,534 bytes over baseline2667412. Its delta from the earlier completed f0fb gate is the app/library handoff and its test, the hosted job limit, and documentation/receipts. Fourteen recorded Git-object boundaries confirm unchanged Core/CLI, inspection/analyze HTTP, corpus inputs/helpers, all five corpus-layer tests, orchestration, manifests and lockfile. `033-handoff-source-delta.json` records those identities.

The retained f0fb gate passed `xvfb-run -a npm run ci` in817.043s, standalone `npm run test:corpus` in514.138s, package smoke, installed inspection exports and production Docker smoke, with unchanged source. Its aggregate `033-linux-release-corpus/report.json` is complete and product-conformant. The final4379e7d gate reruns full CI, including all five normative corpus/UX/a11y layer files, then package and Docker checks. It does not regenerate an identical standalone aggregate for the unchanged corpus boundary; the older aggregate keeps its own source identity rather than being relabeled as a final-source report.

That4379e7d CI attempt found one isolated test-fixture dependency regression: the extracted `refreshSamples` row-render fixture omitted the newly referenced `resumeSavedLoad` binding. All existing assertions remain; the fixture now supplies an asynchronous no-pending-selection dependency. Focused disclosure14/14, format and lint pass. The failing run's logs are retained under `033-linux-handoff-initial-*`; only its owned runner was stopped, and no in-flight source was patched. The replacement snapshot changes only that fixture and verification records relative to4379e7d. Runtime bytes are identical, so the amended native journey run remains valid. The replacement full unit suite passes3872 with0 failures; its three declared skips are transport-only corpus cases whose Core validation is not applicable, with transport behavior covered on the HTTP/browser layers.

The replacement immutable gate snapshot is tree `e6e89c17481fda7cd1ea446408fd86c908b88cfc`, patch SHA256 `95b8e2cee86d3cda02edc128bf1dd35bccb8fd018faabb063c88a1144c48a1ed`,1,167,352 bytes. `033-linux-handoff-final-source.json` binds the exact files and versions.

The amended native journey run now passes all40 grouped tests: ten strengthened saved/history groups on each of Chrome151.0.7922.137 and Firefox155.0.1 at1366px and1920px, with0 failures and0 skips. The four earlier navigation rows now have actual selected-editor-body proof, including all three locales, locked reload and negative/race controls. `native-matrix/journey-results-server-handoff.json` SHA256 `e3a4ab08840510daafbde248cd29caa8cf65bad112f581981e2ca998412200e3` preserves exact4379e7d source hashes and commands. Its frozen ledger is40 pass/0 fail/20 Safari open. The separate24 mapping cells retain their unchanged-code execution receipt. Current matrix state is reconciled accordingly; unavailable Safari/VoiceOver and six physical rows remain explicit.

## Final Linux release gate

The immutable e6e89c1 / 95b8e2ce snapshot passes the full required gate on vkbox. `xvfb-run -a npm run ci` completes in850.480s:189 unit files with3872 pass,0 fail and3 declared transport-only Core N/A;31 serial browser files with347 pass,0 fail and0 skips. All five normative corpus/UX/a11y layer files pass within this run, including browser259/259. The complete saved/history file passes10/10 on its first attempt. There is no retry in this successful run.

Package smoke, a fresh packed Core install, all three public inspection exports, the14-profile JSON catalog, CLI dependency^0.48.0 and production Docker smoke pass. The Core0.48.0 tarball SHA256 is `2715e8e3c974191bf7774b9000a0b671c4b6008198e910dbf53f3f69e00345e4`. The post-run source tree exactly matches e6e89c1. `033-linux-handoff-final-gates.json` retains commands, timestamps, versions, logs and the unchanged-source proof. The retained f0fb standalone corpus aggregate keeps its original identity as described above.

Independent post-gate comparison found only current033 inventory, matrix and verification receipt differences from the tested source; runtime, tests, packages and workflow are identical. T043 and T044 are verified. Hosted CI for the actual commit, fresh canonical backup and deployment remain pending. T019 retains59 native Safari/VoiceOver cells and six physical rows as open.

## Hosted gate: first actual result and the receipt-path repair

The first hosted run for the pushed commit `311a6e3`,
[34356457091](https://github.com/vladikkrasulya/adtech-spyglass/actions/runs/34356457091), **failed**.
The raised job limit was not the cause and was not reached: the run finished in 15m13 inside the new
25-minute allowance. One assertion failed, `tests/brand-guard.test.js`, and its report is exact.

`layout-results.json` and `matrix-results.json` recorded every receipt and screenshot as an absolute
path inside the authoring laptop's own checkout — a `/Users/…` home directory, the repository folder
named after the retired brand, and a tool-local `.codex` subdirectory. That reintroduced the retired
name into tracked files, which the brand guard exists to refuse, and it wrote one machine's private
checkout path into a Spec Kit artifact, which the project's evidence convention already refuses on
its own terms. The Linux gate could not have caught it: it ran on the same source,
but `npm run ci` was executed before these receipt files reached their final content, and the
independent post-gate comparison recorded exactly that difference as receipts-only.

The repair strips that one leading prefix and nothing else, in 614 places — 543 in `layout-results.json` and
71 in `matrix-results.json`. Every pointer keeps its meaning: the remaining value is the
bundle-relative path the prose already uses, `remaining-questions-20260909/…`, and
`layout-results.json` now states that base explicitly in `evidencePathsRelativeTo`, as
`matrix-results.json` already did for its own two nested receipts. All 540 layout cells, every
`receiptSha256`, `harnessSha256` and recorded source hash are unchanged, and
`matrix-results.manifestSha256` still matches `matrix-manifest.json` byte for byte, because the
frozen manifest never carried an absolute path. No runtime, test, package or workflow byte changes.

The retained external bundles are unaffected; only the tracked pointers to them are now
machine-independent. The durable server-side research references used by earlier packages are a separate, documented
convention and are untouched.

## Evidence location after the 2026-09-09 server-only instruction

The owner directed that no further work runs on the laptop. The repository content is already
entirely on the server and on `origin`: the package, the runtime, the raised job limit and the
version set (app `1.23.0`, Core `0.48.0`, CLI dependency `^0.48.0`) are all present in the pushed
commit, and the tree difference from the gated snapshot is the receipts-only delta this document
already records.

What is not yet on the server is the external evidence bundle the manifests point at —
`remaining-questions-20260909/`, holding the 540 layout screenshots, both native-zoom result sets,
the journey and witness receipts, and the physical-device logs. Until it is copied to the server's
durable research directory, those pointers resolve only on the machine that produced them. The
receipts themselves are unchanged and remain valid for the runs they describe; what is missing is
reach, not integrity. Every later run belongs on the server beside the earlier bundles.

## Release

The repaired commit's hosted run
[34361707124](https://github.com/vladikkrasulya/adtech-spyglass/actions/runs/34361707124) succeeded;
the earlier failure is retained above as the first attempt. A fresh canonical backup was taken
immediately before deployment and both archives were integrity-checked. `scripts/deploy.sh` reports
**DEPLOY OK: v1.23.0 (`e842ad8`)**, with readiness, smoke 19/19, container healthy and RestartCount 0.

Read back after deployment: local and public `/api/health` both report build `e842ad8`; the served
`version.js` reports `v1.23.0`; the public Inspector answers 200 in 0.17s and its HTML contains no
injected Cloudflare beacon, matching the R03 configuration readback. The tag `v1.23.0` is published.

This closes T045 and T046. T019 stays open on its own terms: native Safari and VoiceOver cells and
six physical-device rows remain unavailable rather than inferred, and no later run has changed that.
