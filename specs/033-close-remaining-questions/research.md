# Research and decisions

## Session revival and timing

Temporary real SQLite reproduces a failed delete followed by successful old-cookie hydration. A held synthetic sample response reproduces the test's empty-input Analyze followed by late sample arrival and no verdict; a readiness-based click succeeds. This establishes a concrete timing defect, not the unrecorded cause of the historical hosted run.

Choose durable intent+prearmed recovery fence and versioned keyed DB lookup, with orderly continuity and explicit uncertain-start invalidation. Reject memory-only denial, public-hash rollback keys, log-and-continue recovery and marking shutdown clean before HTTP drain. Independent review additionally requires unknown-cookie write refusal and protection against stale credential proof after concurrent password reset.

## Mapping scope

The existing mapping table has unused version and non-unique lookup index. Choose explicit version2 path rows, empty canonical value, nine roles, exact-first resolution and schema2 export. Reject magic wildcard values or params-only discriminators: old clients may already store them. Confirmed field scope must omit the observed value.

## Creative resources

A full synthetic push document loses head styles because the current inliner returns body HTML only. Reuse classification/escaping/sandbox and the existing raster fetch boundary, extending explicit resource inventory/rewrite to Native, responsive and CSS image slots. Reject automatic advertiser loading, remote script execution and a second renderer.

## Cloudflare and browser evidence

A real Chrome response contains an injected beacon that the repository never emits; curl does not reliably reproduce the conditional injection. Disable automatic Web Analytics injection for this site and verify the saved setting plus browser response. The only available connected browser currently shows the Cloudflare login page; the owner has been asked to sign in while implementation continues. Origin no-transform has broader effects and is not the selected fix.

Safari26.6.2/safaridriver, Chrome and native VoiceOver are installed; Firefox is an obtainable official tool. Real physical-device connectivity and native automation permissions still require execution checks. CSS zoom, pixel-density overrides and computed AX names are not native zoom/screen-reader evidence.

## SChain sources

The [IAB SupplyChain specification](https://github.com/InteractiveAdvertisingBureau/openrtb/blob/main/supplychainobject.md) permits a complete single node and makes rid/name/domain optional. Seller account identity is case-sensitive; seller business domain is not an app bundle. Serialized separators must be handled before field decoding. Duplicate seller identity and conflicting valid copies have bounded diagnostic meaning. No IAB basis establishes a four-node video warning. Declared sender context supplies only declared comparison evidence.

## Dialect witnesses and declared route

The retained PBS revision is `0ba352315253f6692af6497d553cfb12909a1b8b`. All4052 audit files match recursive digest `edf782a09fdf3180feb9077b360d96eceab5a316f12bc342fae6376c118d9230`. Go1.25.0 is verified from official metadata, dependencies prepared separately, and network-none `go mod verify` passed. Direct MakeRequests witnesses avoid upstream auction filters and network calls. Freeze7disagreements+12inconclusive IDs; use exact transformation/fanout functions instead of pretending the old oracle measured them.

008 explicitly permits declared-route relevance when authoritative actual-route records do not exist. Select that alternative with typed profile/direction/revision and declared provenance; no guessed labels or traffic claims. This is the owner's newly authorized product direction, not a retroactive rewrite of the paused audit.

## Official operational references

- [Cloudflare Web Analytics setup](https://developers.cloudflare.com/web-analytics/get-started/) and [FAQ](https://developers.cloudflare.com/web-analytics/faq/).
- [Cloudflare cache-control interactions](https://developers.cloudflare.com/cache/concepts/cache-control/): no-transform also affects compression and other injections, so it is not a narrow substitute.
- [Apple native Safari WebDriver](https://developer.apple.com/documentation/safari-developer-tools/macos-enabling-webdriver) and [isolated automation windows](https://developer.apple.com/documentation/safari-developer-tools/webdriver).

External evidence bundle: `remaining-questions-20260909`; prior032 artifacts remain separate. No production records or secrets are copied into this feature.
