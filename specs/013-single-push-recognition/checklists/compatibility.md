# Compatibility & Regression Requirements Checklist: Single-Object Push Response Recognition

**Purpose**: Validate that the requirements for the push single-object recognition change are
complete, unambiguous, and internally consistent on the six axes the owner's request names:
public-contract stability, detection precedence, alias parity, locale parity, synthetic-only
evidence, and shared-worktree/release constraints — before tasks and implementation.
**Created**: 2026-08-26
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [contract](../contracts/feed-push-single.md)

**Note**: This checklist is generated by the `$speckit-checklist` command based on feature context and requirements.

## Public-Contract Stability

- [x] CHK001 Is every finding ID whose firing conditions change enumerated by name, with its ID, severity, and text explicitly pinned as unchanged? [Completeness, Contract §What changes 2-3]
- [x] CHK002 Is the new result-type string spelled out verbatim, with the naming convention it follows and the compatibility class (additive) stated? [Clarity, Contract §What changes 1]
- [x] CHK003 Are deterministic finding order and deduplication semantics explicitly carried into the non-regression list rather than assumed? [Completeness, Contract §What must NOT change]
- [x] CHK004 Is the Core SemVer consequence of the additive contract change stated with its trigger and bump size? [Completeness, Contract §Versioning; Spec assumption on scope]
- [x] CHK005 Does the spec distinguish "a finding stops firing on valid input" (intended fix) from "silent contract change", and is the explicit-decision requirement of Constitution IV satisfied in a tracked artifact? [Consistency, Contract §What changes 3]

## Detection Precedence

- [x] CHK006 Is the full precedence order enumerated as a testable table, including where the new branch sits (last before heuristics)? [Measurability, Data-model §Precedence]
- [x] CHK007 Are the shapes that must keep their current classification listed exhaustively (oRTB 2.x/3.0, URL request, clickunder/link-feed wrappers, JSON Feed 1.1, three unique-key single-bid vendors, array feeds)? [Coverage, Spec FR-002]
- [x] CHK008 Is the collision case "unique-key vendor payload that also carries push-like keys" (e.g. bid-price with `link` + `title`) explicitly resolved in favor of the vendor signature, in both detection and format discrimination? [Edge Case, Spec §Edge Cases; Research R3]
- [x] CHK009 Is the non-claim boundary specified precisely enough to test (which key combinations stay `unknown`, e.g. `link` alone, `title`+`link` without a price key)? [Clarity, Spec FR-001 + §Edge Cases]
- [x] CHK010 Is the behavior for genuinely unknown payloads (ID, severity, text of `payload.unknown_type`) pinned as unchanged? [Completeness, Spec assumption; Contract §What changes 2]

## Alias Acceptance Parity

- [x] CHK011 Is the alias set closed and enumerated pair-by-pair, with a stated rule for aliases not on the list (out of scope until observed)? [Completeness, Spec FR-003 + Assumptions]
- [x] CHK012 Is "identically in the single-object and list forms" backed by a structural requirement (shared validation body) rather than only a test aspiration? [Consistency, Spec FR-004; Research R3]
- [x] CHK013 Is the semantics of an alias present with a wrong-typed value specified (existing finding still fires, same as canonical key)? [Edge Case, Research R4]
- [x] CHK014 Is the both-names-present case resolved (canonical validated, no duplicate finding)? [Edge Case, Spec §Edge Cases]
- [x] CHK015 Are the price-field type tiers (`bid_string_type` / `bid_not_numeric` / `bid_required`) explicitly required to behave identically in both shapes? [Coverage, Spec US2 scenario 3; Contract §What must NOT change]

## Locale Parity

- [x] CHK016 Does the feature state whether any message keys are added or changed, and is the parity obligation (en/ru/uk in the same change) tied to that answer? [Completeness, Spec FR-006; Research R6]
- [x] CHK017 Is the mechanism that would catch locale drift named (existing parity audit test), so "zero message changes" is verifiable rather than asserted? [Measurability, Plan §Constitution VI]
- [x] CHK018 Is the reused-message path (`num` = 1 in single form) checked against message texts that embed the material number, so wording stays sensible for a single object? [Edge Case, Research R3/R6]

## Synthetic-Only Evidence

- [x] CHK019 Do all user stories, FRs, and validation guides consistently require a synthetic replica (same keys/shapes, synthetic values) rather than the production payload, with the constitution clauses cited? [Consistency, Spec US1/FR-007; Quickstart §1]
- [x] CHK020 Is the synthetic replica itself specified concretely enough to be reproducible (key set, value shapes, example values)? [Measurability, Quickstart §1]

## Shared-Worktree & Release Constraints

- [x] CHK021 Are the peer-owned dirty paths enumerated, with the staging rule (authored paths only, never `git add -A`) and the blocked actions (push, deploy) stated as stop conditions rather than workarounds? [Completeness, Plan §Constitution VIII]
- [x] CHK022 Is the release path after the tree settles described (standing authorization, gates, backup, exact-SHA deploy) with the stop condition named per the constitution's "stop and say which one" rule? [Coverage, Plan §Constitution VIII]

## Notes

- All 22 items pass against the artifacts as written on 2026-08-26; no spec/plan amendments
  were required by this checklist beyond the synthetic-evidence amendment already applied
  during planning.
- CHK018 rationale: `feed.push.*` texts read "Push material #1: …" for the single form —
  acceptable and consistent; no wording change required.
