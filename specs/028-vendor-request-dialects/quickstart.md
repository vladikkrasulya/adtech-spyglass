# Quickstart: Verify the Core vendor portion

1. Work only in the isolated feature branch based on the confirmed PR #83 main landing. Read this package, the constitution, roadmap and relevant baseline contracts; use its ignored machine-local feature pointer.
2. Capture the actual `loadCorpus()` baseline for all 256 cases and separately identify the five-group/24-case scope plus five supplemental DEF-151 cases. Preserve payloads and exact expectations/signatures for comparison.
3. Run the new public-boundary tests first. During implementation, use the existing package scripts and narrow `node --test` selections for vendor validation, decoder, format and malformed-input controls; record red-before-green evidence where adding behavior.
4. Replay the selected cases through the existing Core and real HTTP corpus adapters, retaining all unrelated behavior. An Adon3 result must visibly contain the provisional warning even when its parsing/format observations are usable.
5. Before changing metadata, compare the full 256-case baseline using the real loader and unchanged evaluator. Keep unresolved browser lines. The two Kadam assertions require the recorded explicit decision; DEF-180 preview assertions belong to Opus.
6. Run the applicable browser layer with the project's `unshare` Chrome isolation recipe and synthetic fixtures. Preserve existing sealed CSP, blocked navigation and material identity checks; no public source changes are assigned here. A complete individual retirement requires a current full browser pass.
7. Reconcile Core 0.46.0, CLI 0.1.3 with dependency `^0.46.0` and lock metadata. Read back all version surfaces after integration; preserve the app's independent version.
8. Run formatting, type/lint, public finding reference/locale, version/package and governance checks required by the changed paths, then `npm run ci`. Stage only authored paths, push once for the settled wave, and record hosted CI for that exact revision.

Use current runner output for command details, totals and limits. Store raw observations, source snapshots, server ports and Chrome namespace receipts in machine-local evidence. Tracked verification records may contain concise commands, outcomes, public source links and synthetic case IDs, without private absolute paths or payload bodies. A Core/HTTP pass is not a preview pass, registry publication or deployment.
