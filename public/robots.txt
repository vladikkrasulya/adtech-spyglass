# Spyglass · OpenRTB Inspector — robots.txt
# Public landing + docs are open for indexing. Auth-gated API endpoints + the
# /api/proxy harness aren't worth crawling.

User-agent: *
Allow: /
Disallow: /api/

Sitemap: https://spyglass.kyivtech.com.ua/sitemap.xml
