# Тримовний аудит виводів (en / uk / ru) — 2026-08-27

> **Historical snapshot (2026-08-27).** This audit preserves the findings and severity
> classification available at that date; it is the evidence base for
> [015-trilingual-output-parity](../specs/015-trilingual-output-parity/spec.md), not a live defect
> tracker. Use the [platform baseline](../specs/000-platform-baseline/plan.md) for current behavior
> and the [Spec Kit roadmap](../specs/ROADMAP.md) for active work.

Метод: 10 агентів-сканерів (Sonnet) прочитали кожну поверхню, що несе локаль;
10 агентів-скептиків (Sonnet) намагалися спростувати кожну знахідку проти реальних файлів.
Плюс детермінований скрипт паритету ключів і скрипт розрізнення абеток (і/ї/є/ґ проти ы/ъ/э/ё).

**Підтверджено 68** · P0 23 · P1 18 · P2 27 · відхилено 2

Паритет ключів як такий **чистий**: 291 ключ × 3 локалі в `public/i18n.js` без пропусків,
нуль розбіжностей у наборах `{плейсхолдерів}`, нуль контамінації абеток. Усі дефекти —
там, куди тест на паритет не дивиться.

---

## P0 — користувач бачить чужу мову

### `modules-catalog-b`

- **`lib/label-persona.js:84`** — runtime-locale-ignored · зламано для: ru, en
  - рядок: `Відповідай виключно JSON. reason — українською, одне-два речення, з посиланням на конкретний доказ.`
  - проблема: The system persona sent to Ollama on every POST /api/dialects/suggest-label call hardcodes the model's `reason` field to Ukrainian. No caller in the chain (public/modules/inspector/dialect-label.js askAgent() at line 356 -> modules/ai-label/handler.js -> lib/ollama.js classifySignal() at line 165, which passes `system: PERSONA` verbatim at line 180) ever reads or forwards a locale — grepping the whole path for 'locale' returns nothing. The resulting Ukrainian reason is rendered raw via `esc(suggestion.reason || '')` at public/modules/inspector/dialect-label.js line 207, sitting next to properly localized chrome (`t('dialect.label.proposal_title')`, `t('dialect.label.confidence')`, `t('dialect.label.source....hint')` at lines 361-386) that comes from the same object.
  - фікс: Thread the active UI locale through askAgent() -> POST body -> handleSuggest() -> classifySignal(), and have lib/label-persona.js build its closing instruction from a locale map (uk/ru/en) instead of the fixed Ukrainian literal on line 84.
- **`packages/core/dialects/signal-lexicon.js:272`** — hardcoded · зламано для: ru, en
  - рядок: `Ключ «${key}» — vendor-прапорець, який pop-мережі надсилають замість оголошення формату; движок читає його як ознаку pop.`
  - проблема: resolveSignal() is the deterministic first-stage resolver that (per modules/ai-label/handler.js's own header) answers 'the majority' of signals with no model call. Every reason it produces is a hardcoded Ukrainian template literal, confirmed recurring at lines 258-272 (reason at 272 shown), 286, 303-306, 336, 345, 380-386, 392, 404 — grep for 'reason:' in the file shows exactly this set, all plain JS string interpolation with zero i18n involvement (no require of any i18n module, no t() calls anywhere in the file). handler.js line 163-167 returns `fromLexicon` as `suggestion` directly in the JSON 200 response with no transformation, reaching the same `.dl-reason` / esc(suggestion.reason) rendering path at dialect-label.js:207 as the model-path finding above.
  - фікс: Replace each pre-rendered reason string with a structured payload (reasonKey + reasonParams) plus uk/en/ru entries in the client's i18n module, and render through t(reasonKey, reasonParams) instead of the raw Ukrainian sentence signal-lexicon.js currently returns.

### `hardcoded-frontend-a`

- **`public/ortbtools.app.js:4406`** — hardcoded · зламано для: all
  - рядок: `$('statusText').textContent = 'backend offline';`
  - проблема: When the analyze backend is unreachable, the status pill is set with a bare English literal while the adjacent line 4403 correctly localizes via t('status.local'). A ru/uk visitor sees English 'backend offline' next to a localized entity label.
  - фікс: Add an i18n key (e.g. 'status.backend_offline') with en/uk/ru values in public/i18n.js and use $('statusText').textContent = t('status.backend_offline');
- **`public/shell-boot.js:165`** — hardcoded · зламано для: all
  - рядок: `<h1 style="margin:0 0 8px;">Module activation failed</h1>`
  - проблема: When a section module throws during activate(), the SPA replaces #app-root with this inline error page. The heading and the raw ${err.message} beneath it are hardcoded English with no locale branching, unlike updateSectionTitle() in the same file which resolves document.documentElement.lang.
  - фікс: Localize via the same document.documentElement.lang lookup used in updateSectionTitle() (shell-boot.js:184), adding an i18n key for 'Module activation failed' with uk/ru translations.
- **`public/shell-boot.js:153`** — hardcoded · зламано для: all
  - рядок: `<p>No section registered for <code>${canonical}</code>.</p>`
  - проблема: The SPA's inline 404 fallback renders this sentence in English unconditionally, with no en/uk/ru branching anywhere in the file for it.
  - фікс: Localize via the same document.documentElement.lang lookup used in updateSectionTitle(), with an i18n key for the 404 body text in en/uk/ru.
- **`public/ortbtools.app.js:2779`** — hardcoded · зламано для: all
  - рядок: `title="Jump to this path in the JSON"`
  - проблема: Every finding card with a JSON path renders a jump button whose tooltip is a hardcoded English literal built directly into the HTML string, never going through t().
  - фікс: Add an i18n key (e.g. 'finding.jump_to_path') and interpolate escapeHtml(t('finding.jump_to_path')) into the title attribute.
- **`public/ortbtools.app.js:2791`** — hardcoded · зламано для: all
  - рядок: `title="OpenRTB spec reference">spec ↗</a>`
  - проблема: Every finding with a specRef renders a link whose tooltip ('OpenRTB spec reference') and visible label ('spec ↗') are both hardcoded English literals, not looked up via t().
  - фікс: Add i18n keys for both the tooltip and the link label (e.g. 'finding.spec_tooltip', 'finding.spec_label') and interpolate t() results instead of the literals.
- **`public/modules/admin-blog/index.js:203`** — hardcoded · зламано для: all
  - рядок: `if (!confirm('Reject this draft?')) return;`
  - проблема: The reject-draft confirmation dialog is a hardcoded English literal passed straight to confirm(), even though this module localizes every button label via the L/pick() table.
  - фікс: Add an L key (e.g. L.confirmReject) with en/uk/ru text and call confirm(pick(L.confirmReject, lang)).
- **`public/modules/admin-blog/index.js:218`** — hardcoded · зламано для: all
  - рядок: `alert('Promoted! ' + result.hint);`
  - проблема: After successfully promoting a draft, the success alert is built from a hardcoded English prefix ('Promoted! ') concatenated with server data, never going through the module's own L/pick() localization.
  - фікс: Add an L key (e.g. L.promotedPrefix) and build the alert as alert(pick(L.promotedPrefix, lang) + result.hint).
- **`public/modules/admin-blog/index.js:243`** — hardcoded · зламано для: all
  - рядок: `if (e.name !== 'AbortError') alert('Error: ' + e.message);`
  - проблема: Network/API failures in adminPost() surface via a hardcoded English 'Error: ' prefix instead of a localized label.
  - фікс: Add an L key (e.g. L.errorPrefix) and localize: alert(pick(L.errorPrefix, lang) + e.message).

### `hardcoded-frontend-b`

- **`public/modules/intel/builder.js:472`** — hardcoded · зламано для: all
  - рядок: `' fields · score '`
  - проблема: Cluster-suggestion card header joins field count and score with raw English "fields"/"score" via string concat, bypassing t(); shown in every locale's Discovery Builder modal whenever clusters.length > 0.
  - фікс: Add builder.cluster_meta key ('{n} полів · оцінка {score}' uk / '{n} полей · оценка {score}' ru / '{n} fields · score {score}' en) and render via t(), matching the pattern already used for t.useCluster etc. on the same line.
- **`public/modules/intel/builder.js:524`** — hardcoded · зламано для: all
  - рядок: `' total</span>'`
  - проблема: Every row of the field list ends with raw English "total" concatenated directly, not via t() — rendered for every observation row in the modal, all locales.
  - фікс: Add builder.field_total key ('{n} всього' / '{n} всего' / '{n} total') and build the meta string with t().
- **`public/modules/intel/builder.js:646`** — hardcoded · зламано для: all
  - рядок: `'Rule-based purpose: ' + r.purpose + ' (confidence: ' + r.confidence + ')'`
  - проблема: row.title tooltip set to raw English text regardless of active locale after the field-purpose lookup succeeds and the user hovers a field row.
  - фікс: Add builder.field_purpose_tooltip key and render via t() with {purpose, confidence} params, for all three locales.
- **`public/modules/intel/builder.js:676`** — hardcoded · зламано для: all
  - рядок: `'Custom ' + new Date().toISOString().slice(0, 16).replace('T', ' ')`
  - проблема: Blank dialect-name field defaults to English-prefixed 'Custom YYYY-MM-DD HH:MM', persisted as spec.name and later displayed verbatim as the card title on /dialects for every locale.
  - фікс: Localize the default-name prefix via t('builder.default_name_prefix') (uk: 'Власний', ru: 'Свой', en: 'Custom') before falling back to it.
- **`public/modules/save-sample/index.js:92`** — hardcoded · зламано для: all
  - рядок: `currentSampleMeta.title || 'sample'`
  - проблема: Updating a saved sample with an empty stored title pre-fills the Save modal's title input with the literal English word 'sample', regardless of locale.
  - фікс: Route through a new t('sample.default_title') key, localized uk/en/ru.
- **`public/modules/save-sample/index.js:99`** — hardcoded · зламано для: all
  - рядок: `j.id || j.site?.domain || j.app?.bundle || 'sample'`
  - проблема: New save with no id/site.domain/app.bundle in the pasted payload defaults the title field to the English word 'sample' for every locale.
  - фікс: Same fix as line 92/313 — route through t('sample.default_title').
- **`public/modules/save-sample/index.js:313`** — hardcoded · зламано для: all
  - рядок: `$('mTitle').value.trim() || 'sample'`
  - проблема: confirmSave() falls back to the literal English word 'sample' as the persisted title on empty submit, later shown in the Library list for every locale.
  - фікс: Route through the same localized default-title key as the other two occurrences in this file.
- **`public/modules/library/index.js:433`** — hardcoded · зламано для: all
  - рядок: `s.title || '(no title)'`
  - проблема: Saved sample with empty/null title renders the literal English '(no title)' as its row link label in /library, regardless of active locale.
  - фікс: Add a library.no_title key (or reuse the existing cabinet.untitled key: '(без назви)'/'(без названия)'/'(untitled)' already defined at public/i18n.js:1228) and use it instead of the bare literal.
- **`public/modules/simulate/index.js:90`** — hardcoded · зламано для: all
  - рядок: `j.error || 'simulation_failed'`
  - проблема: success:false response with no ollama_unavailable code and no j.error passes the bare literal 'simulation_failed' straight to renderFailure(), which writes it via escapeHtml(msg) with zero t() wrapping — the entire error message shown is raw English, in every locale.
  - фікс: Add a modal.simbids.failed_generic key (all 3 locales) and use `j.code === 'ollama_unavailable' ? t('modal.simbids.ollama_down') : (j.error || t('modal.simbids.failed_generic'))`.

### `backend-output`

- **`email.js:95`** — runtime-locale-ignored · зламано для: all
  - рядок: `verifyTemplate(): subject: 'Підтвердіть свою адресу — ortbtools' (line 99); resetTemplate(): subject: 'Скидання паролю — ortbtools' (line 117)`
  - проблема: verifyTemplate (line 95-111) and resetTemplate (line 113-129) hardcode Ukrainian subject/html/text with no locale parameter in their signatures. sendVerifyEmail/sendResetEmail (lines 160-176) take only (user, token, baseUrl) — no locale. Confirmed their only callers, modules/auth/handler.js lines 123, 277 (verify) and 339 (reset), never read a cookie, query param, or the user's preferred_locale (present on the row per server.js:918 publicUser projection) before calling — grep for resolveLocale/kt-lang/locale in handler.js turns up nothing wired to the email calls. Every verification and reset email is Ukrainian for every user regardless of site language or saved preference.
  - фікс: Thread a locale argument through sendVerifyEmail/sendResetEmail → verifyTemplate/resetTemplate, sourced in modules/auth/handler.js from user.preferred_locale (fallback to the kt-lang cookie for anonymous-adjacent flows), and give the templates an {en, uk, ru} dict matching packages/core/messages/*.json's pattern.

### `bot-ai-surfaces`

- **`lib/label-persona.js:84`** — runtime-locale-ignored · зламано для: en, ru
  - рядок: `Відповідай виключно JSON. reason — українською, одне-два речення, з посиланням на конкретний доказ.`
  - проблема: This is the final sentence of the PERSONA template literal (verified byte-for-byte at line 84), sent verbatim as the `system` field on every /api/generate call in lib/ollama.js classifySignal() (confirmed at ollama.js:180, `system: PERSONA`). It hardcodes 'reason — in Ukrainian' with zero locale parameterization anywhere in the file or its exported surface (module.exports = { PERSONA } is a bare constant, not a function). classifySignal() takes no locale argument (confirmed by reading its full signature and body), so this line deterministically forces every model-path suggestion.reason to be Ukrainian for every user regardless of site locale. Language claim verified: 'українською', 'посиланням', 'доказ' — genuine Ukrainian-only vocabulary/orthography, not a uk/ru homograph.
  - фікс: Turn PERSONA into buildPersona(locale) or a PERSONA_BY_LOCALE map varying only this closing sentence; keep every other line byte-identical across locales so scripts/label-calibration.js's HOLDOUT comparison stays valid, then re-run that script once after adding the localized variants.
- **`public/modules/inspector/dialect-label.js:207`** — wrong-language · зламано для: en, ru
  - рядок: `esc(suggestion.reason || '')`
  - проблема: Confirmed as the concrete render site: every sibling string in the same proposal card — proposal_title (197), source badge (185/187), confidence label (201), source hint (210), low_confidence_warn (212) — goes through t()/esc(t(...)), while suggestion.reason alone is injected raw via esc() with no i18n lookup. A companion i18n module (public/modules/inspector/dialect-label.i18n.js) exists specifically for this card's other strings, confirming the omission is real and inconsistent, not an intentional design choice. Because suggestion.reason is sourced either from the hardcoded-Ukrainian model path (lib/label-persona.js:84, lib/ollama.js) or the hardcoded-Ukrainian lexicon path (packages/core/dialects/signal-lexicon.js), a ru- or en-locale user genuinely sees a Ukrainian sentence embedded in an otherwise-localized modal. This is 'wrong language actually rendered' per the severity rubric — P0 confirmed as-is.
  - фікс: No change needed at this line once the upstream sources (persona, ollama.js, handler.js, signal-lexicon.js) become locale-aware — it already renders whatever `reason` the server returns.
- **`packages/core/dialects/signal-lexicon.js:272`** — runtime-locale-ignored · зламано для: en, ru
  - рядок: `reason: `Ключ «${key}» — vendor-прапорець, який pop-мережі надсилають замість оголошення формату; движок читає його як ознаку pop.`,`
  - проблема: resolveSignal() is Stage 1 — the deterministic lexicon path that, per modules/ai-label/handler.js's own docstring, 'Resolves the majority ... with no model call at all.' Confirmed multiple additional hardcoded-Ukrainian `reason:` template literals in the same function (e.g. lines 258-259, 286, 303-306, 336, 345, 380-383, 392, 404), all returned directly as `suggestion.reason` and passed straight to sendJson at handler.js:165-169 with zero locale handling, then rendered raw by the same unguarded esc(suggestion.reason) call site confirmed in the finding above. Because this is the majority path (no GPU/model involved at all), it is arguably the primary source of visible wrong-language text, not a secondary one — P0 confirmed. Language claim verified: 'прапорець', 'мережі', 'оголошення', 'читає' are genuine Ukrainian-only forms.
  - фікс: Convert each reason template into a per-locale lookup (a local REASONS(locale) table inside signal-lexicon.js, keyed like the module i18n catalogs), thread the `locale` param added to resolveSignal() through to whichever branch fires. Not constrained by scripts/label-calibration.js (that benches the model's label/confidence choice, not the lexicon's fixed wording), so it can be localized without any recalibration step.

---

## P1 — локаль губиться дорогою або мовчазний фолбек

### `modules-catalog-a`

- **`public/modules/dialects/i18n.js:4`** — dead-code-misleading-comment · зламано для: all
  - рядок: `Loaded at module bootstrap; merged into the global I18N object so `t('dialects.btn.new')` resolves anywhere.`
  - проблема: Header comment (lines 4-5) falsely claims this file's uk/en/ru string tables are merged into the global I18N object via t(). In reality this file uses bare `export const uk/en/ru = {...}` and never calls window.registerI18nModule(...) or pushes onto window.kt_i18n_modules, unlike every sibling module i18n.js (corpus-save, edit-sample, diff, auth, embed, share, stream, macros all do this at their tails). A repo-wide grep for 'dialects/i18n' finds only the file itself and its own README; public/shell-boot.js line 60 dynamic-imports only modules/dialects/index.js, never i18n.js. index.js instead resolves all its copy through its own local `L` map via `pick(map, lang)` (confirmed at index.js lines 36, 95, 218, 269, 563+). The module's own README.md states this explicitly at line 78-82: 'this module does not go through window.t / public/i18n.js' and 'modules/dialects/i18n.js is a leftover from an abandoned CRUD design and is imported by nothing.' So all 30-ish keys x 3 locales in this file are inert dead code with a misleading integration claim.
  - фікс: Delete public/modules/dialects/i18n.js (the live catalog is index.js's L map; the README already documents this file as abandoned dead code), or if repurposed, wire it in with the same registerI18nModule/kt_i18n_modules tail every sibling file has, import it from index.js, delete the duplicate L map, and correct the header comment.

### `html-shell`

- **`public/modules/inspector/template.ru.html:332`** — wrong-language · зламано для: ru
  - рядок: `aria-label="Toggle theme"`
  - проблема: The theme-toggle button's accessible name is left in English on the Russian template while the equivalent control in en.html:326 is correctly English, uk.html:332 is correctly 'Перемкнути тему', and the sibling nav/index.js pick() localizes the same control elsewhere as 'Переключить тему'.
  - фікс: Change to aria-label="Переключить тему", matching nav/index.js's pick({en:'Toggle theme', uk:'Перемкнути тему', ru:'Переключить тему'}).
- **`public/index.ru.html:239`** — hardcoded · зламано для: ru
  - рядок: `<aside id="kt-nav-root" class="kt-nav" aria-label="Sections"></aside>`
  - проблема: The outer <aside> landmark's aria-label is hardcoded English 'Sections' in all three locale files and is never touched by mountNav(), which only sets innerHTML; the nested <nav> it injects is correctly localized to 'Разделы' via pick().
  - фікс: Either drop the redundant static aria-label (the inner <nav aria-label> already supplies an accessible name) or set it per-locale and have mountNav() sync root's own aria-label at mount time.
- **`public/index.uk.html:244`** — hardcoded · зламано для: uk
  - рядок: `<aside id="kt-nav-root" class="kt-nav" aria-label="Sections"></aside>`
  - проблема: Same defect as index.ru.html:239 — outer landmark aria-label hardcoded English 'Sections', never localized by mountNav(), while the nested <nav> is correctly 'Розділи'.
  - фікс: Same as ru.html: drop the redundant static aria-label, or set aria-label="Розділи" and sync it from JS at mount time.

### `hardcoded-frontend-a`

- **`public/account.js:853`** — hardcoded · зламано для: all
  - рядок: `'<span class="matrix-cell matrix-num" title="True Positive">TP</span>' + ... title="False Positive" ... title="False Negative" ... title="True Negative"`
  - проблема: The confusion-matrix table header localizes every other header cell via T('matrix.col.pattern'), T('matrix.col.precision'), T('matrix.col.recall'), but the TP/FP/FN/TN tooltip explanations are hardcoded English strings.
  - фікс: Add i18n keys for the four tooltip strings (e.g. 'matrix.tp_tooltip' etc.) with uk/ru translations and use T() in place of the literal title attributes.
- **`public/modules/admin-blog/index.js:197`** — hardcoded · зламано для: all
  - рядок: `container.innerHTML = `<p class="ablog-error">Error: ${escapeHtml(e.message)}</p>`;`
  - проблема: The draft-list fetch-failure state renders a hardcoded English 'Error:' label into the admin panel body, unlike every other label in this module which is localized through L/pick().
  - фікс: Add an L key for the error-label prefix and render escapeHtml(pick(L.errorPrefix, lang)) instead of the literal 'Error:'.

### `hardcoded-frontend-b`

- **`public/modules/intel/builder.js:298`** — hardcoded · зламано для: all
  - рядок: `aria-label="Close"`
  - проблема: Discovery Builder modal's icon-only close button has a hardcoded English aria-label, inconsistent with the rest of the app which localizes every other aria-label (topbar, nav, insights, library, dialects all route theirs through t()/pick()), and with the existing btn.close i18n key ('закрити'/'close'/'закрыть') used by other modals' close buttons.
  - фікс: Add a builder.close_aria key with uk/ru/en values and set the attribute via t('builder.close_aria') in ensureRoot().
- **`public/modules/mirror/index.js:206`** — hardcoded · зламано для: all
  - рядок: `j.error || 'mirror_failed'`
  - проблема: success:false with no j.error throws an Error whose message is the bare literal 'mirror_failed'; rendered at line 218 appended after the localized t('modal.mirror.failed') prefix, so the user sees a localized phrase followed by a raw English token, e.g. uk 'не вдалось згенерувати: mirror_failed'.
  - фікс: Fall back to a localized generic message instead of the raw code, e.g. `throw new Error(j.error || t('modal.mirror.failed_generic'))`, with modal.mirror.failed_generic added for uk/en/ru.
- **`public/modules/library/index.js:467`** — hardcoded · зламано для: all
  - рядок: `pick(T.loadFailed, lang) + ': ' + state.catalogError`
  - проблема: state.catalogError is e.message from `throw new Error('catalog HTTP ' + r.status)` in fetchCatalog (line 540) — a hardcoded-English technical string concatenated after the localized 'Could not load' prefix, for every locale.
  - фікс: Store/display a translated-generic reason instead of the raw Error.message; keep e.message only for console.warn/debugging.

### `backend-output`

- **`modules/auth/handler.js:58`** — hardcoded · зламано для: all
  - рядок: `const EMAIL_UNSENT_PUBLIC_MSG = 'Email provider error — try again in a few minutes.';`
  - проблема: This constant is set as email_error at lines 128, 274, 288 and rendered client-side at public/ortbtools.app.js:5450 as toast(t('toast.send_failed', { error: j.email_error || '' })). toast.send_failed's own prefix is fully localized (confirmed in public/i18n.js: uk 'Не вдалося відправити: {error}', ru 'Не удалось отправить: {error}'), but {error} always carries this English constant, producing a mixed-language toast for uk/ru users.
  - фікс: Move EMAIL_UNSENT_PUBLIC_MSG into packages/core/messages/{en,uk,ru}.json (or public/i18n.js) and resolve it by locale before setting email_error, the same way toast.send_failed's own prefix is resolved.
- **`modules/gists/handler.js:213`** — runtime-locale-ignored · зламано для: all
  - рядок: `sendError(res, 410, 'gist_expired', 'This share link has expired') (line 213); sendError(res, 404, 'not_found', 'No such share link') (lines 197, 208)`
  - проблема: These sendError message strings become body.error on the wire. public/modules/share/index.js:241 (and :221) does throw new Error(body.error || 'HTTP ' + resp.status), caught at line 361-363 and shown as toastErr(tt('toast.share_gist_invalid', { error: e.message })). toast.share_gist_invalid's own text is fully localized (public/modules/share/i18n.js:105-109, all three locales present), but {error} always carries the server's raw English fragment, so a uk/ru user who opens an expired or bad share link reads a mixed-language toast.
  - фікс: Map the gist_expired/not_found code to a localized string client-side (the pattern humanAuthError()/humanResetError() already use elsewhere) instead of interpolating the server's raw error text.
- **`modules/sample/handler.js:60`** — runtime-locale-ignored · зламано для: all
  - рядок: `const label = pick.replace(/^synthetic-/, '').replace(/\.json$/, '').replace(/-/g, ' ');`
  - проблема: handleSample (line 60) and handleSampleList (labels built the same way, note = raw._note) derive label/note purely from the on-disk filename/fixture, always English, with zero locale awareness — confirmed handler.js imports only fs/path/http, no resolveLocale anywhere in the file. public/modules/library/index.js:402 renders item.label as the primary clickable title (titleCell) and item.note as its hover tooltip verbatim for every locale, while the same row's FORMAT_LABEL/CATEGORY_LABEL chips (confirmed lines 397-398, pick(..., lang)) are properly localized — so a uk/ru catalog row shows a translated category chip next to an always-English title/tooltip.
  - фікс: Give each samples/*.json fixture an {en, uk, ru} label/note (or a slug-keyed lookup in packages/core/messages) and have handleSampleList resolve it against the request locale before returning, the way extractAllCategories(payload, locale) does in modules/analyze/handler.js (confirmed that pattern exists at handler.js:299-300).

### `bot-ai-surfaces`

- **`lib/ollama.js:166`** — runtime-locale-ignored · зламано для: en, ru
  - рядок: `const lines = [`Шлях: ${signalPath}`, `Значення: ${JSON.stringify(signalValue)}`];`
  - проблема: classifySignal()'s user-turn prompt is built entirely in Ukrainian ('Шлях:', 'Значення:', and 'Сусідні ключі в тому самому ext:' / 'Структура impression:' / 'Сигнал на рівні запиту — impression відсутній.' a few lines below at 168/172-173), with no locale input to the function. 'Шлях' and 'Значення' are genuine Ukrainian-only words (Russian would be 'Путь'/'Значение'), so the language claim holds. Downgraded from the reported P0: this line is never itself rendered to a user — it only shapes the model's input context — and the guaranteed-Ukrainian output of the reason field is already fully explained by lib/label-persona.js:84 (finding above), which is sufficient on its own regardless of what language these field labels are in. This is a real, verified contributing/reinforcing issue, not an independently sufficient cause of visible wrong-language text.
  - фікс: Accept a `locale` param on classifySignal({..., locale}) and swap these structural field labels for a small per-locale table, ideally living next to lib/label-persona.js so the two travel together. No calibration impact expected (structural labels, not classification instructions), but re-run scripts/label-calibration.js once after the change.
- **`modules/ai-label/handler.js:186`** — runtime-locale-ignored · зламано для: en, ru
  - рядок: `const suggestion = await ollama.classifySignal({`
  - проблема: The finding's original quote at line 143 was a paraphrased composite, not a verbatim line (line 143 is only `body = await readJson(req);`); corrected the anchor to the concrete call site. handleSuggest() parses signal_path (line 148), signal_value (line 155) and imp (line 160), calls resolveSignal({signalPath, signalValue, imp: body.imp}) at line 163, and calls ollama.classifySignal({signalPath, signalValue, impSketch, siblingKeys}) at line 186 — neither call is ever given a locale, and body.locale/body.lang is never read anywhere in the file. This is the genuine, verified point where a per-request locale should enter the pipeline and does not. The app has an established pattern for exactly this elsewhere (public/modules/behavior/behavior-tab.js calls /api/analyze-behavior?locale=..., and public/i18n.js documents /api/analyze?locale=... resolving server-side), which this route conspicuously does not follow. Downgraded from P0: by itself this line/omission renders nothing; it is necessary plumbing for a fix, not itself a wrong-language render.
  - фікс: Read `const locale = ['en','uk','ru'].includes(body.locale) ? body.locale : 'uk';` after parsing body, thread it into resolveSignal({..., locale}) and ollama.classifySignal({..., locale}), mirroring the existing ?locale= pattern used by /api/analyze and /api/analyze-behavior.
- **`public/modules/simulate/index.js:130`** — hardcoded · зламано для: uk, ru
  - рядок: `escapeHtml(s.reason || '')`
  - проблема: Confirmed sibling defect: modal.simbids.strat.* and modal.simbids.bid/pass go through t() (lines 120/123) while s.reason is rendered raw. Confirmed s.reason is produced by lib/intel-rules.js simulateBids() as plain hardcoded English template literals (verified verbatim at lines 495, 504, 516, 525, 540, 549 — e.g. line 495 `reason: \`floor ${floor} ${cur} beyond scale budget (cap 25)\``), with modules/intel/handler.js:198 calling `intelRules.simulateBids(parsed)` with no locale argument. This is real: public/modules/simulate/i18n.js shows this exact modal has a full, working uk/en/ru translation table for every other string in the card, making the untranslated reason field a genuine, verifiable gap. Downgraded from P0 to P1: unlike the Ukrainian-in-Russian-UI defect above, this is English (the app's baseline/dev language) leaking into uk/ru locales — a silent fallback to another supported language rather than an unrelated third language — which the severity rubric treats as P1, and it is invisible to en-locale users entirely.
  - фікс: Thread a locale from the request body through modules/intel/handler.js's handleIntelSimulateBids into intelRules.simulateBids(parsed, locale), and localize the six reason templates in lib/intel-rules.js the same way as signal-lexicon.js above.

### `locale-plumbing`

- **`public/i18n.js:1297`** — runtime-locale-ignored · зламано для: all
  - рядок: `const tpl = (I18N[lang] && I18N[lang][key]) || I18N.uk[key];`
  - проблема: window.t() falls back to the Ukrainian string for ANY locale (including en, the canonical no-prefix locale) whenever a key is missing from that locale's table, with no warning or visible marker distinguishing it from a legitimate Ukrainian string.
  - фікс: Prefer en before uk as the fallback and console.warn on every fallback so drift is visible in dev: hit = I18N[lang]&&I18N[lang][key]; tpl = hit || (lang!=='en' && I18N.en[key]) || I18N.uk[key]; warn if !hit.
- **`server.js:78`** — runtime-locale-ignored · зламано для: en
  - рядок: `const DEFAULT_LOCALE = 'uk';`
  - проблема: resolveLocale() (server.js:940-944) uses this uk default whenever POST /api/analyze's ?locale= is missing or invalid, silently returning Ukrainian finding text to any integrator that omits the param — contradicting the canonical-en convention used elsewhere in the same codebase.
  - фікс: Change to 'en' to match public/core/routes.js:41 and packages/core/categories.js:26, or document the uk exception explicitly and echo the resolved locale in a response header.
- **`packages/core/messages/index.js:23`** — runtime-locale-ignored · зламано для: all
  - рядок: `const FALLBACK_LOCALE = 'uk'; ... const tpl = dict[id] || fallback[id];`
  - проблема: resolve() (index.js:38-41) falls back to the uk catalog for ANY requested locale, including en, when a finding id is missing from that locale's JSON — the same silent uk-fallback mechanism as the client i18n.js.
  - фікс: Try the requested locale, then en, then uk as last resort, and rely on a strengthened parity test to catch drift at CI time.

---

## P2 — якість, регістр, метадані

### `core-catalog`

- **`public/i18n.js:1235`** — quality · зламано для: uk,ru
  - рядок: `'cabinet.status.warn_pct': { en: 'warn {pct}%', uk: 'warn {pct}%', ru: 'warn {pct}%' }, 'cabinet.status.err_pct': { en: 'err {pct}%', uk: 'err {pct}%', ru: 'err {pct}%' },`
  - проблема: In the same cabinet status-mix row (public/account.js:429-435, pill('ok',clean_pct)+pill('warn',warn_pct)+pill('danger',err_pct)) the sibling key cabinet.status.clean_pct is translated (uk/ru 'чисто {pct}%' at line 1234) but warn_pct and err_pct are byte-identical to English in both uk and ru. A uk/ru cabinet user sees a mixed-language pill row, e.g. 'чисто 80% · warn 15% · err 5%'. No module i18n.js overrides these keys. 'warn'/'err' are generic English abbreviations, not shared OpenRTB technical vocabulary, so the exemption in rule 5 does not apply.
  - фікс: Translate to match the sibling's pattern: uk 'warn {pct}%' -> 'варнінг {pct}%' (or similar), 'err {pct}%' -> 'помилки {pct}%'; ru 'warn {pct}%' -> 'предупр. {pct}%', 'err {pct}%' -> 'ошибки {pct}%'.
- **`public/i18n.js:143`** — quality · зламано для: uk
  - рядок: `'behavior.empty': 'Відрендер креатив справа — Behavior-двигун аналізує спроби клік-джекінгу, бот-патерни, маніпуляції з фреймами та важкі креативи.'`
  - проблема: 'Відрендер' is a truncated stem, not a valid Ukrainian 2nd-person imperative (should be 'Відрендери'). The parallel en value opens with imperative 'Render a creative...' and ru (line 775) correctly uses imperative 'Отрендерь креатив справа...'. The identical broken opening phrase 'Відрендер креатив справа' also appears hardcoded, user-visible, in public/modules/inspector/template.uk.html:872 inside a live '.empty-hint' div (the Behavior tab's empty state), confirming it is copy-pasted user-facing text, not a comment or dead code.
  - фікс: Change to a valid imperative, e.g. 'Відрендери креатив справа — Behavior-двигун аналізує спроби клік-джекінгу, бот-патерни, маніпуляції з фреймами та важкі креативи.' and correct the same phrase in template.uk.html:872.
- **`public/i18n.js:99`** — quality · зламано для: uk
  - рядок: `'toast.corpus_delete_failed': 'Не вдалось видалити: {error}' (also line 1015 toast.sample_load_failed, line 1194 toast.decrypt_failed_with_hint)`
  - проблема: The uk catalog uses the standard form 'вдалося' 12 times for the same '...не вдалося...' failure-message pattern (copy_failed, copy_failed_select, save_failed, save_changes_failed, delete_failed, decrypt_failed, send_failed, merge_partial, merge_failed, verify_failed, and both partner/query load-failure toasts) but switches to the colloquial contraction 'вдалось' in exactly 3 keys: toast.corpus_delete_failed (line 99), toast.sample_load_failed (line 1015), toast.decrypt_failed_with_hint (line 1194). All are genuine user-facing toast strings, not comments/dead code.
  - фікс: Normalize the 3 outliers to 'вдалося': 'Не вдалося видалити: {error}', 'Не вдалося завантажити приклад', 'Не вдалося розшифрувати цей запит. Найімовірніше сесія застаріла — вийди й увійди ще раз.'
- **`public/i18n.js:689`** — quality · зламано для: ru
  - рядок: `'toast.signed_out': 'Вы вышли из аккаунта'`
  - проблема: Line 689 is the only capitalized-'Вы' (formal register) string in the entire ru catalog, while at least 19 other ru strings use informal 'ты'-register imperatives (войди, вставь, сохрани, выбери, нажми, скопируй, удали, запусти — e.g. line 685 'Не удалось расшифровать — войди в аккаунт ещё раз' right above it). This breaks register consistency, though it plausibly exists to dodge gendered past-tense agreement (вышел/вышла), a pattern mirrored identically at uk line 46 'Ви вийшли з акаунту'.
  - фікс: If gender-avoidance wasn't the intent, rephrase without the formal pronoun, e.g. ru: 'Сессия завершена' / uk: 'Сесію завершено', both gender-neutral and register-consistent.

### `html-shell`

- **`public/index.en.html:33`** — html-parity · зламано для: en
  - рядок: `<meta property="og:locale:alternate" content="uk_UA" />`
  - проблема: index.en.html declares only one og:locale:alternate (uk_UA), missing ru_RU, while index.ru.html correctly declares both en_US and uk_UA as alternates.
  - фікс: Add <meta property="og:locale:alternate" content="ru_RU" /> alongside the existing uk_UA tag.
- **`public/index.uk.html:36`** — html-parity · зламано для: uk
  - рядок: `<meta property="og:locale:alternate" content="en_US" />`
  - проблема: index.uk.html declares only one og:locale:alternate (en_US), missing ru_RU, unlike index.ru.html which lists both other locales.
  - фікс: Add <meta property="og:locale:alternate" content="ru_RU" /> alongside the existing en_US tag.
- **`public/index.ru.html:59`** — hardcoded · зламано для: ru
  - рядок: `"description": "Free OpenRTB validator with request↔response crosscheck, IAB Content Taxonomy decoding, and creative preview. Your bid data is sent over HTTPS, analyzed on the server, and never stored`
  - проблема: The JSON-LD description field is byte-identical English source text, while the sibling meta description, og:description and twitter:description in the same file are correctly translated to Russian a few lines above.
  - фікс: Localize the JSON-LD description field to match the page's own og:description, e.g. 'Бесплатный валидатор OpenRTB с перекрёстной проверкой request↔response, декодированием IAB Content Taxonomy и превью креатива. Данные отправляются по HTTPS, анализируются на сервере и не сохраняются.'
- **`public/index.uk.html:64`** — hardcoded · зламано для: uk
  - рядок: `"description": "Free OpenRTB validator with request↔response crosscheck, IAB Content Taxonomy decoding, and creative preview. Your bid data is sent over HTTPS, analyzed on the server, and never stored`
  - проблема: Same issue as index.ru.html:59 — JSON-LD description left as untranslated English source string even though meta/og/twitter descriptions in the same file are correctly Ukrainian.
  - фікс: Localize the JSON-LD description to Ukrainian, matching the page's own og:description.
- **`public/modules/inspector/template.en.html:982`** — html-parity · зламано для: en
  - рядок: `<span class="footer-dialect-value">custom ▾</span>`
  - проблема: The static seed text for the footer dialect readout differs by locale for no linguistic reason: en.html ships 'custom ▾' while ru.html:987 and uk.html:990 both ship 'IAB ▾' — a different default value, not a translation of the same word.
  - фікс: Make all three files' seed value agree (e.g. 'IAB' with no chevron, matching current paintFooterDialect() output) so pre-hydration state is locale-independent.
- **`public/index.en.html:257`** — quality · зламано для: en
  - рядок: `🔒 Explicit retention`
  - проблема: This hero sub-heading is immediately followed by body copy saying data is 'sent over HTTPS, analyzed on the server, and not persisted there' — i.e. explicitly NOT retained. 'Explicit retention' says the opposite of what the sentence means. The parallel ru ('Прозрачное хранение') and uk ('Прозоре зберігання') headings correctly mean 'Transparent storage/handling', consistent with their own body copy.
  - фікс: Reword the EN heading to match the actual claim and its ru/uk counterparts, e.g. '🔒 Transparent handling' or '🔒 No server-side retention'.

### `html-pages`

- **`public/about.en.html:1269`** — html-parity · зламано для: en
  - рядок: `<td>Locale</td><td>UK 🇺🇦</td><td class="ok">Supported</td>`
  - проблема: The Version & format support matrix's Locale sub-table lists only EN and UK. about.uk.html's copy of the same table has a third row for RU, and RU is a genuinely live, shipped locale (public/about.ru.html, account.ru.html, index.ru.html all exist and about.ru.html itself declares og:locale=ru_RU). about.en.html structurally omits the RU row.
  - фікс: Add a third row after the UK row, before </tbody> at line 1273 (not 1272 as originally suggested — verified by direct read): <tr><td>Locale</td><td>RU 🇷🇺</td><td class="ok">Supported</td></tr>.
- **`public/about.ru.html:1270`** — html-parity · зламано для: ru
  - рядок: `<td>Locale</td><td>UK 🇺🇦</td><td class="ok">Поддерживается</td>`
  - проблема: Same support-matrix table on the Russian page itself lists EN and UK as supported but never RU — the only locale of the three not represented in its own doc, even though the reader is currently on the RU page and RU is a real shipped locale.
  - фікс: Add a third row before </tbody> at line 1274 (verified correct): <tr><td>Locale</td><td>RU</td><td class="ok">Поддерживается</td></tr>.
- **`public/about.ru.html:614`** — placeholder-mismatch · зламано для: ru
  - рядок: `<code>ortbtools-{дата}-{6-hex}.json</code>`
  - проблема: The literal filename-pattern placeholder {date} was translated to {дата}, but public/export.js:85 builds the real filename as 'ortbtools-' + date + '-' + hash + '.json' with an actual ISO date substring — never the Cyrillic word — so the documented pattern no longer matches reality.
  - фікс: Keep the token untranslated: <code>ortbtools-{date}-{6-hex}.json</code>, matching public/about.en.html:614 and the real generator in public/export.js.
- **`public/about.uk.html:613`** — placeholder-mismatch · зламано для: uk
  - рядок: `<code>ortbtools-{дата}-{6-hex}.json</code>`
  - проблема: Same placeholder-translation defect as about.ru.html:614 — {date} rendered as {дата}, diverging from the app's real generated filename.
  - фікс: Restore the literal token: <code>ortbtools-{date}-{6-hex}.json</code>.
- **`public/account.ru.html:912`** — quality · зламано для: ru
  - рядок: `сохраняется в ваш аккаунт, когда вы залогинены (server-side, через`
  - проблема: This is the only formal-register (вы/ваш) sentence in the page body; a Unicode-boundary-aware regex scan of the whole file found the only other вы/ваш hit was in the <meta name="description"> tag (line 9) — every second-person reference in the visible body text (lines 666, 827, 851, 876, 987) uses informal ты/твой.
  - фікс: Match the page's informal register: 'сохраняется в твой аккаунт, когда ты залогинен (server-side, через …'
- **`public/account.uk.html:909`** — quality · зламано для: uk
  - рядок: `зберігається на ваш акаунт, коли ви залогінені (server-side, через`
  - проблема: Same register break as the ru file — the only formal ви/ваш sentence in the page body; everything else uses informal ти/твій (lines 664, 702, 824, 848, 873).
  - фікс: Use informal register: 'зберігається на твій акаунт, коли ти залогінений (server-side, через …'
- **`public/about.ru.html:489`** — quality · зламано для: ru
  - рядок: `без передачи ваших данных третьим сторонам`
  - проблема: 'ваших' is the only formal-register possessive in the document; the English source has no possessive here at all ('no third-party data sharing'), so it was added by the translator and clashes with the doc's otherwise informal tone (e.g. line 485 'имеешь дело', line 1069 'твой IP-адрес').
  - фікс: Either drop the possessive to match English ('без передачи данных третьим сторонам') or use informal register: 'без передачи твоих данных третьим сторонам'.
- **`public/about.uk.html:488`** — quality · зламано для: uk
  - рядок: `без передачі ваших даних третім сторонам`
  - проблема: Identical register break as about.ru.html:489 — the only formal 'ваших' in a document that otherwise uses informal ти/твій throughout.
  - фікс: Drop the possessive to match English, or use informal register: 'без передачі твоїх даних третім сторонам'.
- **`public/about.ru.html:41`** — quality · зламано для: ru
  - рядок: `og:image:alt (line 34) 'Вставь bid, увидишь правду.' vs twitter:description (line 41) 'Вставьте bid — он анализируется...'`
  - проблема: Within the same <head>, og:image:alt uses informal imperative 'Вставь…увидишь' (ты-form) while twitter:description switches to formal imperative 'Вставьте' (вы-form) for the same call-to-action, seconds apart in the same document.
  - фікс: Pick one register (the page's dominant informal one): twitter:description → 'Документация ortbtools — бесплатный валидатор OpenRTB. Вставь bid — он анализируется на сервере и не сохраняется.'
- **`public/about.uk.html:40`** — quality · зламано для: uk
  - рядок: `og:image:alt (line 33) 'Встав bid, побачиш правду.' vs twitter:description (line 40) 'Вставте bid — він аналізується...'`
  - проблема: Same intra-file register clash as about.ru.html: og:image:alt is informal 'Встав…побачиш' while twitter:description switches to formal 'Вставте' for the same sentence, in the same <head>.
  - фікс: Use consistent informal register: 'Документація ortbtools — безкоштовний валідатор OpenRTB. Встав bid — він аналізується на сервері й не зберігається.'
- **`public/about.en.html:27`** — html-parity · зламано для: en
  - рядок: `<meta property="og:locale:alternate" content="uk_UA" />`
  - проблема: about.ru.html declares both alternates (en_US and uk_UA via two og:locale:alternate tags, lines 27-28). about.en.html declares only the uk_UA alternate and omits ru_RU entirely, even though public/about.ru.html is a real, live page. about.uk.html has the identical gap (only en_US alternate, no ru_RU, at line 26).
  - фікс: Add a second alternate tag: <meta property="og:locale:alternate" content="ru_RU" /> after line 27 in about.en.html (and the equivalent addition after line 26 in about.uk.html).
- **`public/account.ru.html:607`** — quality · зламано для: ru
  - рядок: `aria-label="Навигация кабинетом"`
  - проблема: This is a calque: bare instrumental case with no preposition reads as broken Russian. Russian's idiomatic pattern for 'X navigation' is 'навигация по X' (compare the extremely common 'навигация по сайту'), not bare instrumental. This aria-label is read aloud by screen readers, so it is genuinely user-facing (to assistive-tech users) despite not being visible body text.
  - фікс: Use a prepositional phrase: aria-label="Навигация по кабинету" (and "Навігація по кабінету" in account.uk.html).

### `backend-output`

- **`packages/core/messages/ru.json:104`** — wrong-language · зламано для: ru
  - рядок: `crosscheck.bid.native_extra_assets: "Сидбід #{sNum} → ставка #{bNum}: в native-ответе есть неожиданные ассеты (id: {extra})…"`
  - проблема: Every other 'Сидбид' occurrence in ru.json (33 of them, confirmed by grep) uses the Russian и; this one key alone spells it 'Сидбід' with Ukrainian і, introduced in commit 5503f80 (confirmed via git blame) and never fixed. The string is genuinely rendered to users — native_extra_assets is consumed by packages/core/crosscheck.js and packages/core/finding-location.js as a real crosscheck finding.
  - фікс: Change 'Сидбід' to 'Сидбид' on line 104 of ru.json to match the other 33 occurrences and keep uk.json's 'Сідбід' (which is correctly Ukrainian) untouched.

### `locale-plumbing`

- **`packages/core/messages/ru.json:104`** — wrong-language · зламано для: ru
  - рядок: `Сидбід #{sNum} → ставка #{bNum}: в native-ответе есть неожиданные ассеты (id: {extra})...`
  - проблема: The word 'Сидбід' mixes a Ukrainian dotted 'і' into an otherwise fully-Russian sentence; every other occurrence in this file (15+) correctly uses 'Сидбид' with plain и.
  - фікс: packages/core/messages/ru.json:104 — change 'Сидбід' to 'Сидбид'.
- **`tests/i18n-audit.test.js:211`** — quality · зламано для: all
  - рядок: `if (!(id in CATALOGS[locale])) continue;`
  - проблема: The parity test only compares {var} placeholders for a finding id that already exists in both en.json and the target locale; an id missing entirely from uk.json/ru.json is silently skipped, and the outer loop (Object.keys(CATALOGS.en)) structurally can't catch an id present in uk/ru but absent from en either.
  - фікс: Add a key-set-parity assertion across all three catalogs before the placeholder-drift loop.
- **`public/i18n.js:1313`** — quality · зламано для: ru
  - рядок: `keys_uk: Object.keys(I18N.uk).length,       keys_en: Object.keys(I18N.en).length,`
  - проблема: window.tInfo(), the debug/introspection helper for this exact machinery, reports key counts for uk and en only, omitting ru even though ru is a co-equal first-class locale in the same table.
  - фікс: Add `keys_ru: Object.keys(I18N.ru).length,` to the object returned by tInfo() at public/i18n.js:1310-1316.
- **`public/modules/intel/banner.js:114`** — runtime-locale-ignored · зламано для: all
  - рядок: `document.body.appendChild(_root);`
  - проблема: The Discovery chip is appended directly to document.body rather than the registry-managed section root, so registry.deactivate()'s DOM sweep never removes it, and its localized text is set only inside refresh() (called on-demand from analyze()) with no kt:lang-change listener to re-render it — so switching site language after the chip has appeared leaves its text frozen in the prior locale.
  - фікс: Either mount the chip inside the registry-managed section root, or add a kt:lang-change listener that re-calls refresh() with the cached last summary, mirroring nav/index.js and topbar/index.js.

---

## Відхилено скептиками

- `public/modules/dialects/i18n.js:1` — Confirmed dead: grep -rn "dialects/i18n" across public/ finds only the file itself and its own README mention; no <script> or import references it; grep for t('dialects.* / t("dialects.* across public/ returns nothing but the file's own header comment. public/modules/dialects/README.md:81-82 already documents this explicitly: "`modules/dialects/i18n.js` is a leftover from an abandoned CRUD design and is imported by nothing." Per audit rule 3 ("REJECT anything that is ... dead code"), this file's content never executes and never reaches a user, so it is out of scope for this audit regardless of its misleading header — rejected on that basis, not on factual accuracy (the facts asserted are true).
- `public/i18n.js:4` — Line 4 quoted verbatim, and the documentation drift is real: activeLocale() (confirmed in file) resolves three locales from <html lang>/data-lang then localStorage, and window.t() (line 1297, see finding 1) does a single-step fallback straight to I18N.uk with no en step. However this is a code comment, never rendered or executed — per audit rule 3 ("REJECT anything that is a comment"), out of scope for this user-facing i18n audit despite being an accurate observation.

## Що читали агенти

### `core-catalog` (4 перевірених тверджень)

Read public/i18n.js in full (1317 lines). Programmatically extracted all 291 keys × 3 locales (Node eval of the file with stubbed window/document/localStorage) and verified: (a) key parity — 291/291 keys present in en, uk, AND ru, including the 4 badge.* keys, which the mechanical pre-scan mis-flagged as en-only — they ARE assigned to I18N.uk/I18N.ru at lines 954-957/960-963, synchronously before window.t is defined, so no P1 missing-key defect exists there (confirmed no fallback-to-uk occurs for any key in practice); (b) placeholder-set parity — zero {var} mismatches across all 291 keys; (c) cross-language letter contamination — zero uk-exclusive letters (їієґ) in ru, zero ru-exclusive letters (ыъэё) in uk, zero Cyrillic in en, across every one of the 291×3 values including the appended tier2/tier3/cab batches. Cross-referenced badge.* and cabinet.status.*_pct callers in public/modules/inspector/request-input.js and public/account.js. This file itself is clean of P0/P1-class defects; the findings below are P2 translation-quality/consistency issues found by side-by-side reading.

### `modules-catalog-a` (1 перевірених тверджень)

Read in full: public/modules/{auth,corpus-save,dialects,diff,edit-sample,embed,macros,migrate,mirror,partners,password-reset}/i18n.js. Cross-checked with public/modules/dialects/README.md, public/modules/dialects/index.js, public/shell-boot.js, and repo-wide grep for every module's i18n import sites (public/i18n.js, public/index.{en,uk,ru}.html, public/ortbtools.app.js, public/core/modal-host.js, tests/*). Ran scripted completeness checks (every key has uk/en/ru) and placeholder-parity checks ({var} token sets equal across the three locale strings for every key) across all 10 IIFE-style files — both passed clean.

### `modules-catalog-b` (2 перевірених тверджень)

Read in full: public/modules/recovery/i18n.js, public/modules/save-sample/i18n.js, public/modules/share/i18n.js, public/modules/shortcuts/i18n.js, public/modules/simulate/i18n.js, public/modules/stream/i18n.js, public/modules/unlock/i18n.js, public/modules/inspector/dialect-label.i18n.js, public/modules/inspector/source-nav.i18n.js. Cross-checked every key's uk/en/ru triple and every {placeholder} set programmatically (no mismatches, no missing ru/en/uk found in any of the 9 files). Followed the "bot's answer" surface named in the task from dialect-label.i18n.js into its consuming code (public/modules/inspector/dialect-label.js askAgent()/openBuilder()), the server route (modules/ai-label/handler.js), the model call (lib/ollama.js classifySignal), the system prompt (lib/label-persona.js), and the deterministic first-pass resolver (packages/core/dialects/signal-lexicon.js) — this is where the reported defect actually lives.

### `html-shell` (9 перевірених тверджень)

public/index.en.html, public/index.ru.html, public/index.uk.html, public/modules/inspector/template.en.html, public/modules/inspector/template.ru.html, public/modules/inspector/template.uk.html, public/lang-switch.js, public/modules/nav/index.js, public/shell-boot.js, public/modules/topbar/index.js (spot checks), public/ortbtools.app.js (paintFooterDialect); structural parity verified via full tag-skeleton diff (0 divergence in <body> across all 3 index files and all 3 templates — 54 and 324 elements respectively, identical tag/id/class sequence) plus a positional dump of every title/placeholder/aria-label/alt/value attribute (63 aligned) and every text node (143 in templates, 8 in index files) across the three locales.

### `html-pages` (12 перевірених тверджень)

public/about.en.html, public/about.ru.html, public/about.uk.html, public/account.en.html, public/account.ru.html, public/account.uk.html — full-file structural diff (id sets, tag counts, table rows, heading/list/button/aria-label inventories via python/grep) plus close reads of every divergent region found; also spot-checked public/account.js (T()-wrapped, no hardcoded strings) since it backs account.html.

### `hardcoded-frontend-a` (10 перевірених тверджень)

—

### `hardcoded-frontend-b` (12 перевірених тверджень)

Read in full: public/modules/inspector/{index,request-input,source-nav,creative-assets,creative-classify,json-syntax,specimen-handoff}.js, public/modules/behavior/{index,behavior-tab}.js, public/modules/dialects/index.js, public/modules/stream/index.js, public/modules/simulate/index.js, public/modules/mirror/index.js, public/modules/library/index.js, public/modules/corpus-save/index.js, public/modules/save-sample/index.js, public/modules/partners/index.js, public/modules/unlock/index.js, public/modules/auth/index.js, public/modules/password-reset/index.js, public/modules/recovery/index.js, public/modules/intel/builder.js, public/modules/intel/observer.js. Cross-checked public/i18n.js for the `builder.*` keys to confirm no existing key was simply left unwired.

### `backend-output` (5 перевірених тверджень)

packages/core/messages/{en,uk,ru}.json (full 344/345-key diff, placeholder-parity check, and letter-class scans for і/ї/є/ґ vs ы/ъ/э/ё vs Cyrillic-in-en across every value) + index.js; email.js; notify.js; lib/landings.js + lib/seo.js (SECTION_SEO, IAB_TAX, BLOG_NOT_FOUND_LABEL, OG_LOCALE — scanned for cross-locale letter leaks); packages/core/categories.js + iab-categories.{en,uk,ru}.json (395/396 parity, extra key is `_locale` metadata, no wrong-language hits) and its caller modules/analyze/handler.js; packages/core/rules/price-floor/index.js and packages/core/behavior/rules/static.js and packages/core/dialects/signal-lexicon.js (no inline user-facing prose — clean, all return ids); every modules/_/handler.js for `sendError(...)` calls with hardcoded prose, cross-checked against the public/modules/_/index.js + i18n.js consumers that either map error `code` to a translated string or fall through to the raw server `message`/`error` field.

### `bot-ai-surfaces` (6 перевірених тверджень)

lib/label-persona.js, modules/ai-label/handler.js, lib/ollama.js, lib/openrouter.js, public/modules/inspector/dialect-label.js, public/modules/inspector/dialect-label.i18n.js, public/modules/dialects/index.js, lib/news-moderator.js, packages/core/dialects/signal-lexicon.js, public/i18n.js, public/modules/simulate/index.js, modules/intel/handler.js, lib/intel-rules.js

### `locale-plumbing` (9 перевірених тверджень)

public/i18n.js (full, incl. header + activeLocale/window.t tail); public/lang-switch.js (full); public/core/routes.js (full); lib/locale-routes.js (full); server.js (locale-relevant sections: setLocaleCookie/readLocaleCookie ~230-292, LOCALE_REDIRECT_TABLE + serveStaticFile ~484-660, publicUser/resolveLocale/resolveDialect ~908-950); packages/core/messages/index.js (full); packages/core/messages/{en,uk,ru}.json (key-set + content diffed programmatically); packages/core/categories.js (DEFAULT_LOCALE); tests/i18n-audit.test.js (full); public/core/registry.js (full); public/shell-boot.js (kt:lang-change handling); public/modules/*/i18n.js (all 18 files, loaded via vm sandbox and diffed for missing/mismatched locale values); public/modules/dialects/{i18n.js,index.js} (full); public/modules/intel/banner.js (full); public/modules/behavior/behavior-tab.js and ortbtools.app.js locale-relevant sections (activeLocale, analyzeUrl, renderBehaviorTab, pluralKey); public/account.js and public/modules/{nav,topbar}/index.js (kt:lang-change + inline locale-dict spot checks); lib/seo.js (LANGS/hreflang/x-default sections).
